hasIssue=true | no service password-encryption
why: Szyfrowanie haseł jest wyłączone, dlatego hasła w sekcjach line pozostają jawne w konfiguracji.

hasIssue=true | enable password cisco
why: Używane jest starsze hasło enable zamiast silniejszego mechanizmu enable secret.

hasIssue=false | interface GigabitEthernet0/0 ->  ip address 192.168.10.1 255.255.255.0
why: Brak zaplanowanego błędu polityki bezpieczeństwa ani logiki sterowania.

hasIssue=false | interface GigabitEthernet0/1 ->  ip address 192.168.20.1 255.255.255.0
why: Brak zaplanowanego błędu polityki bezpieczeństwa ani logiki sterowania.

hasIssue=false | interface GigabitEthernet0/1 ->  ip access-group 110 in
why: Zastosowanie listy ACL samo w sobie nie jest błędem; zaplanowanym błędem jest przesłanianie reguł wewnątrz listy ACL 110.

hasIssue=false | interface GigabitEthernet0/2 ->  no ip address
why: Brak zaplanowanego błędu polityki bezpieczeństwa ani logiki sterowania.

hasIssue=false | interface GigabitEthernet0/2 ->  shutdown
why: Brak zaplanowanego błędu polityki bezpieczeństwa ani logiki sterowania.

hasIssue=false | interface Vlan1 ->  no ip address
why: W tym scenariuszu brak zaplanowanego błędu polityki bezpieczeństwa ani logiki sterowania.

hasIssue=false | interface Vlan1 ->  shutdown
why: W tym scenariuszu brak zaplanowanego błędu polityki bezpieczeństwa ani logiki sterowania.

hasIssue=true | access-list 110 permit ip any any
why: Ta szeroka reguła permit przesłania późniejszą regułę deny dla SSH.

hasIssue=true | access-list 110 deny tcp any any eq 22
why: Reguła deny dla SSH jest nieskuteczna, ponieważ wcześniejsza reguła permit ip any any dopasowuje się jako pierwsza.

hasIssue=true | line con 0 ->  password admin123
why: Hasło konsoli jest przechowywane w postaci jawnego tekstu i jest słabe.

hasIssue=false | line con 0 ->  login
why: Polecenie login prawidłowo włącza uwierzytelnianie konsoli; problemem jest oddzielne słabe hasło zapisane jawnym tekstem oraz wyłączone szyfrowanie haseł.

hasIssue=false | line aux 0
why: Dostęp AUX nie należy do zaplanowanych błędów w tym scenariuszu.

hasIssue=true | line vty 0 4 ->  password telnet123
why: Hasło VTY jest przechowywane w postaci jawnego tekstu, jest słabe i umożliwia niezabezpieczony dostęp zdalny.

hasIssue=true | line vty 0 4 ->  login
why: W bloku VTY brakuje ustawień transport input ssh oraz kontroli access-class, dlatego zdalny dostęp administracyjny nie jest ograniczony według protokołu ani źródła.
