hasIssue=false | no service password-encryption
why: To polecenie nie jest częścią zaplanowanych błędów strukturalnych ani błędów odwołań w tym scenariuszu.

hasIssue=true | interface GigabitEthernet0/1 ->  ip access-group 100 in
why: Interfejs używa listy ACL 100, ale lista ACL 100 nie jest zdefiniowana.

hasIssue=false | interface GigabitEthernet0/0 ->  ip address 10.0.0.1 255.255.255.0
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=false | interface GigabitEthernet0/0 ->  ip nat outside
why: Prawidłowe oznaczenie NAT outside; brak zaplanowanego błędu.

hasIssue=false | interface GigabitEthernet0/1 ->  ip address 192.168.1.1 255.255.255.0
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=false | interface GigabitEthernet0/1 ->  ip nat inside
why: Prawidłowe oznaczenie NAT inside; brak zaplanowanego błędu.

hasIssue=false | interface GigabitEthernet0/2 ->  no ip address
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=false | interface GigabitEthernet0/2 ->  shutdown
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=false | interface Vlan1 ->  no ip address
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=false | interface Vlan1 ->  shutdown
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=true | ip nat inside source list NAT_TRAFFIC interface GigabitEthernet0/0 overload
why: Konfiguracja NAT odwołuje się do niezdefiniowanej listy ACL lub obiektu NAT_TRAFFIC.

hasIssue=true | access-list 150 remark Legacy Internet Edge Filter
why: Lista ACL 150 jest zdefiniowana, ale nie jest nigdzie używana w konfiguracji.

hasIssue=true | access-list 150 deny tcp any any eq telnet
why: Ten wpis należy do zdefiniowanej, ale nieużywanej listy ACL 150.

hasIssue=true | access-list 150 deny tcp any any eq ftp
why: Ten wpis należy do zdefiniowanej, ale nieużywanej listy ACL 150.

hasIssue=true | access-list 150 deny tcp any any eq www
why: Ten wpis należy do zdefiniowanej, ale nieużywanej listy ACL 150.

hasIssue=true | access-list 150 permit ip any any
why: Ten wpis należy do zdefiniowanej, ale nieużywanej listy ACL 150.

hasIssue=false | line con 0
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=false | line aux 0
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.

hasIssue=false | line vty 0 4 ->  login
why: Brak zaplanowanego błędu strukturalnego ani błędu odwołania.
